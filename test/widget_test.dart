import 'package:flutter_test/flutter_test.dart';
import 'package:uit_mobile/main.dart';

void main() {
  testWidgets('UitMobileApp can be instantiated', (WidgetTester tester) async {
    // Verify UitMobileApp exists and can be constructed.
    expect(const UitMobileApp(), isA<UitMobileApp>());
  });
}
